{
    "scripts": {
        "start": "npm-run-all --parallel start:bcmi start:cms",
        "start:cms": "npm run --prefix cms strapi start",
        "start:bcmi": "npm run --prefix bcmi start"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
